<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >  
<mapper namespace="com.enn.commodity.synergistic.dao.OrgInfoDao" >  
 <select id="SelectOrgInfoByObject" resultType="com.enn.commodity.synergistic.entity.OrgInfo">
     SELECT id,name,source,identify
     FROM orginfo
     WHERE 1=1
     <if test="name!=null and name!=''" >
     and name  LIKE CONCAT(CONCAT('%', #{name}), '%')
     </if>
     <if test="id!=null and id!=''" >
     and id=#{id}
     </if>
    <if test="source!=null and source!=''" >
     and source=#{source}
     </if>
    <if test="identify!=null and identify!=''" >
     and identify=#{identify}
     </if>
     
  </select>
 
 <insert id="CreateOrgInfo"  keyColumn="id" parameterType="com.enn.commodity.synergistic.entity.OrgInfo">
    insert into orginfo 
    (id,name,source,identify) 
    values
    (#{id},#{name},#{source},#{identify})
  </insert>
 
 <update id="UpdateOrgInfo" parameterType="com.enn.commodity.synergistic.entity.OrgInfo">
     UPDATE orginfo
     <set>
       <if test="name!=null">name=#{name},</if>
       <if test="source!=null">dataType=#{dataType},</if>
       <if test="identify!=null">identify=#{identify}</if>
       
     </set>
     WHERE id=#{id}
  </update>
 
 <delete id="DeleteOrgInfo" parameterType="com.enn.commodity.synergistic.entity.OrgInfo">
     DELETE FROM
     orginfo
     WHERE
     id=#{id}
  </delete>
 
  <!-- <select id="queryAresById" resultType="com.yaw.demo.entity.Area">
     SELECT areaId,areaName,priority,createTime,lastEditTime
     FROM area
     WHERE areaId=#{areaId}
  </select>
  
  <insert id="insertArea" useGeneratedKeys="true" keyProperty="areaId" keyColumn="areaId" parameterType="com.yaw.demo.entity.Area">
    insert into area (areaName,priority,createTime,lastEditTime) values(#{areaName},#{priority},#{createTime},#{lastEditTime})
  </insert>
  
  
  
  <delete id="deleteArea">
     DELETE FROM
     area
     WHERE
     areaId=#{areaId}
  </delete> -->
</mapper>